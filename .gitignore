*.pdf
*.fdb_latexmk
*.fls
*.aux
*.log
*.bbl
*.blg
*.pag
*.gz
.coverage
.pylintrc
.pytest_cache
.readthedocs.yaml
.uv-cache
.venv
.vscode
*.out
*.toc
*.pyc
*.nav
docs/_build/*
docs/_api/*
*/__pycache__/*
*.egg-info
build/*
dist/*
todo.md
discopy/version.py
benchmark-results/
plans/

# docs/neural: the study keeps its code and figures, but not the downloaded
# benchmarks or the trained checkpoints, which the scripts regenerate.
!docs/neural/figures/*.pdf
docs/neural/sudoku_data/
docs/neural/artifacts/
docs/neural/*.pt
docs/neural/__pycache__/

# local-only compatibility shim for optuna searches launched before the
# experiments package was split into core/ and sudoku/; delete when idle.
docs/neural/experiments/

# optuna study storage (sqlite dbs, journal files + their .lock files, worker
# logs): host-specific run state, not something to version.
docs/neural/optuna/runs/
docs/optuna/runs/
*.db
*.journal
*.lock

# marimo's per-notebook working state
__marimo__/
