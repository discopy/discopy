# TODO

> no, it must be presented as in https://docs.discopy.org/en/interaction-readme/
> the architecture should be a paragraph at the end with the table, on the
> index page

- [ ] move the table to the end of `docs/index.rst`, with its paragraph
- [ ] drop the embedding from the `discopy.abc` docstring
- [ ] restore `docs/api/architecture.html` to its root-relative links, which
      are the ones the index page needs
