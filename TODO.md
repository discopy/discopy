# TODO

> Read desire/EVENING.md and run a 🌙 Evening turn for tonight.

`split/1` took `main`'s eleven commits of 09-07 tonight, so this head is 19 behind
its own base with three conflicts — `CHANGELOG.md`, `discopy/abc.py` and
`test/testing.py`. A head that conflicts with its base is broken, not merely behind.

- [WIP] @253abef6-2026-09-08 01:40 Merge `split/1-axiom-infra` in and resolve the three conflicts
- [ ] Run `pflake8`, the test suite and `proptest/` on the merge result
- [ ] Push and confirm the head is mergeable and CI green
