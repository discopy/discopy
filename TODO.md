# TODO

> Read desire/EVENING.md and run a 🌙 Evening turn for tonight.

An interactive session carried `split/1` down into `split/2` on 09-10 and pushed
`76b8f320` green, so the vocabulary restoration the last round priced as a night
is done and **the ripple's front moves here**. This head is 26 commits behind its
own base with thirteen conflicted paths: `discopy/abc.py`, `proptest/categories.py`
and eleven `test/` modules. A head that conflicts with its base is broken, not
merely behind, and #661–#669 all wait on this one merge.

- [x] Merge `split/2-monoidal-strategy` in and resolve the thirteen conflicts
- [x] Run `pflake8`, the test suite and `proptest/` on the merge result
- [WIP] @01UJ1bCP-2026-09-11 01:00 Push and confirm the head is mergeable and every expected check green
