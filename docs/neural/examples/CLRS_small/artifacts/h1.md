| `floyd_warshall` | seeds | parameters | ID `n = 16` | OOD `n = 64` (32 traj.) | OOD `n = 64` (128 traj.) ± s.e.m. | ± 95% CI (traj.) |
|---|---|---|---|---|---|---|
| edge state | 3 | 384121 | 0.8888 | 0.0741 | 0.0728 ± 0.0141 | ± 0.0020 |
| node only | 3 | 383095 | 0.8033 | 0.1927 | 0.1912 ± 0.0283 | ± 0.0039 |
| **difference** | | | | | **-0.1183 ± 0.0317** | |

Welch `t = -3.74` on `df = 2.9`, and the exact two-sided permutation test over the 20 relabellings of the seeds gives `p = 0.10`. With three seeds an arm the permutation floor is `p = 0.10` however cleanly the arms separate, so a delta here is suggestive and cannot be more than that: a standard-error count is not a significance, and at these degrees of freedom the two-sided 95% threshold is near `|t| = 3.2` rather than `1.96`.

| `matrix_chain_order` | seeds | parameters | ID `n = 16` | OOD `n = 64` (32 traj.) | OOD `n = 64` (128 traj.) ± s.e.m. | ± 95% CI (traj.) |
|---|---|---|---|---|---|---|
| edge state | 3 | 402616 | 0.9887 | 0.3834 | 0.3459 ± 0.0898 | ± 0.0289 |
| node only | 3 | 401590 | 0.9890 | 0.4049 | 0.3901 ± 0.0158 | ± 0.0261 |
| **difference** | | | | | **-0.0442 ± 0.0912** | |

Welch `t = -0.48` on `df = 2.1`, and the exact two-sided permutation test over the 20 relabellings of the seeds gives `p = 1.00`. With three seeds an arm the permutation floor is `p = 0.10` however cleanly the arms separate, so a delta here is suggestive and cannot be more than that: a standard-error count is not a significance, and at these degrees of freedom the two-sided 95% threshold is near `|t| = 3.2` rather than `1.96`.

