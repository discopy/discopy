# TODO

> no, it must be presented as in https://docs.discopy.org/en/interaction-readme/
> the architecture should be a paragraph at the end with the table, on the
> index page

- [x] move the table to the end of `docs/index.rst`, with its paragraph
- [x] drop the embedding from the `discopy.abc` docstring
- [x] restore `docs/api/architecture.html` to its root-relative links, which
      are the ones the index page needs
