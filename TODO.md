# TODO

Review round: USER, in session.

> do we still need style-review as label? no-style-review makes sense tho

- [x] drop the `style-review` label: the comment does everything it did, and
      hands over to the correctness reviewer where the label did not
- [x] update the `CHANGELOG.md` entries and the pull request description
