have a look at and merge the base branch (split/1-axiom-infra) and main (CHANGELOG.md), to update the branc, and make the simplifications that were enabled by recent changes
work locally don't push anything just yet.

- [ ] Merge the latest base branch and main locally, preserving both changelogs.
- [ ] Express the monoidal laws and strategies through the base's category-based axiom infrastructure, preserving their mathematical domains and counterexamples.
- [ ] Simplify redundant declarations and generation using the updated infrastructure; update concise documentation and tests.
- [ ] Run lint, unit/doctests and the property suite; report any remaining failures.

The user's local-only instruction overrides the push requirement for claims.
