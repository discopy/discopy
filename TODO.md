# TODO

Prompt ([#429](https://github.com/discopy/discopy/issues/429), verbatim):

> Tracking a former `TODO` comment removed from `draw_controlled_gate` in #428.
>
> In `discopy/drawing/backend.py`, the nodes for the controlled part of a `Controlled` gate are built with `x=box.dom[0]`, i.e. the first wire of the whole box, regardless of where the controlled sub-gate actually sits:
>
> https://github.com/discopy/discopy/blob/add8d65c31018e92f1682927de0d4c92ad7d737f/discopy/drawing/backend.py#L373-L375
>
> The comment noted that the `x` coordinate should be selected properly for classical gates. Reproducing with a `Controlled` gate over classical wires should show the misplacement.
>
> The wire from the control dot to the target boundary is drawn with `bend_in=True, bend_out=True`:
>
> https://github.com/discopy/discopy/blob/add8d65c31018e92f1682927de0d4c92ad7d737f/discopy/drawing/backend.py#L444-L445
>
> The comment noted that `bend_in`/`bend_out` should be changed for the TikZ backend, whose bending presumably renders this wire differently from matplotlib. Comparing `draw(to_tikz=True)` output of a `Controlled` gate against the matplotlib output should show the discrepancy.

---

- [x] Reproduce the misplacement with a `Controlled` gate over classical wires
- [x] Select the controlled sub-gate's x-coordinate from the wire it actually sits on instead of `box.dom[0]`
- [x] Compare `draw(to_tikz=True)` against the matplotlib output and adjust `bend_in`/`bend_out` for the TikZ backend
- [x] Add regression tests and regenerate the affected docs images
- [x] Run `pflake8 discopy` and `coverage run -m pytest`

## Merge main in (🌙 evening, 2026-07-28)

Branch was 14 commits behind `main`; merged `origin/main` in per RULES.md rule 3
(merge, not rebase). Conflicts: `docs/_static/hypergraph/{box,diagram}.svg`
(unrelated generated baselines, regenerated by #469's reproducibility fix after
we last touched them) resolved by taking `main`'s version, confirmed byte-identical
after a fresh doctest run. `test/drawing/imgs/controlled-classical.png` hit a
file-location conflict because #470 renamed `test/drawing/imgs/` to
`docs/_static/drawing/` and moved every drawing baseline from PNG/doctest-decorator
tests into SVG doctest examples in the `discopy/drawing/drawing.py` docstring
Gallery; the branch's PNG had no home in the new layout. Followed that convention:
added the classical-controlled diagram (both distances) as a new Gallery entry
next to `long-controlled`, regenerated it as
`docs/_static/drawing/controlled-classical.svg`, and dropped the file at its old
path. `test/drawing/drawing.py` had a real content conflict from `draw_and_compare`
(removed by #470) versus our `test_draw_controlled_classical`/
`test_draw_long_box_name`; kept `classical_controlled()` and
`test_tikz_controlled_classical` (TikZ backend still needs its own regression
test, `draw_and_compare` had no TikZ equivalent), dropped
`test_draw_long_box_name` as a duplicate of main's own new `long-box-name.svg`
Gallery entry, and kept main's `test_rich_display`. `discopy/drawing/backend.py`
auto-merged cleanly, diffed it against both parents to confirm the fix (indexed
`box.dom[index[0]]`/`controlled_box.dom[i]` lookups, `len(self.nodelayer) + 1`
node ids) survived intact.

`pflake8 discopy`: clean. `coverage run -m pytest` (`--continue-on-collection-errors`,
since `--ignore` doesn't suppress files matched by `testpaths` first): 526 passed,
102 failed, 8 collection errors, all of them pre-existing missing optional deps
in this sandbox (`sympy`, `jax`, `pytket`, `pennylane`, `tensornetwork`) or a
missing Graphviz binary (`test/cmap.py::test_draw_plain_path`) — none touch
drawing, controlled gates or hypergraph.
