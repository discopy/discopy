# TODO

USER, 2026-09-16, in the interactive session, on `583bb5f3` of
https://github.com/discopy/discopy/pull/400:

> there must be a way to get rid of these custom classes altogether, they look like pure boilerplate slop

then, on the plan to interpret a categorial grammar where it lives, with a `categorial.Functor` into abstract terms, and keep only the closed reading:

> drop them all

- [WIP] @session_01JvjjihGD4ybHGeqLXyLqUi-2026-09-16 11:05 `CategorialType`, `CategorialBox`, `CategorialFunctor`, `StringFunctor` and `Lexicon.from_categorial` deleted; `Diagram.from_categorial` and `Ty.from_categorial` are the closed reading, one functor application, crossed composition composing.
- [WIP] @session_01JvjjihGD4ybHGeqLXyLqUi-2026-09-16 11:05 The tests interpret the same derivations through a string lexicon written per word, a `categorial.Functor` into abstract terms, and check that crossed composition composes.
- [WIP] @session_01JvjjihGD4ybHGeqLXyLqUi-2026-09-16 11:05 The module docstring, the changelog and `to_abstract` say so.
