# TODO

Review round: USER, on #634.

> better than tagging let's add a label `no-style-review`

- [x] make the opt-out the `no-style-review` label instead of a comment,
      dropping the conversation scan and its write-access check, which the
      label gets for free
- [x] update the `CHANGELOG.md` entry, #638 and the pull request description
